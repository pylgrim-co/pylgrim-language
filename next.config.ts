import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // P0 is a throwaway build (see .pylgrim work item phase-0-prove-the-weave).
  // No experimental flags, no custom webpack: the default pipeline is the point.
};

export default nextConfig;
